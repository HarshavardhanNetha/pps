#include <stdio.h>
#include <string.h>
char str[10],name[10];
int i; 
void main(){
//gets(str);
//gets(name);

scanf("%s",str);
//scanf("%s",name);

i=strlen(str);  //siignts second to firstt (new,old)
	printf("%d\n",i);
}
